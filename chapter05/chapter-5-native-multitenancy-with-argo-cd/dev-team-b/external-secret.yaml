# References secret-store.yaml and declares which entries to fetch and
# materialize as a Kubernetes Secret. Git only ever holds this reference,
# never the plaintext value - the same Argo CD repo-credential Secret that
# gitrepository-sealed.yaml produces via sealed-secrets, produced here via
# the External Secrets Operator instead.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: application-dev-team-b
  namespace: dev-team-b
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: dev-team-b-azure-keyvault
    kind: SecretStore
  target:
    name: application-dev-team-b
    template:
      metadata:
        labels:
          argocd.argoproj.io/secret-type: repository
      data:
        url: "{{ .url }}"
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: url
      remoteRef:
        key: dev-team-b-application-url
    - secretKey: username
      remoteRef:
        key: dev-team-b-application-username
    - secretKey: password
      remoteRef:
        key: dev-team-b-application-password
